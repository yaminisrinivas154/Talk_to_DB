const reportWebVitals = () => {
  // This file is intentionally left blank to resolve a build issue.
  // The web-vitals library was not found and is not critical for app functionality.
};

export default reportWebVitals;